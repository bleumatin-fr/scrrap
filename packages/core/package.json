{
  "name": "@scrrap/core",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "start": "tsc -w --preserveWatchOutput",
    "build": "rm -Rf dist && tsc && cp package.json dist && cd dist && yarn install --production"
  },
  "dependencies": {
    "excel-column-name": "^1.0.1",
    "hyperformula": "^2.5.0",
    "mongoose": "^6.4.2",
    "string-to-color": "^2.2.2",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.0/xlsx-0.20.0.tgz"
  },
  "devDependencies": {
    "@types/excel-column-name": "^1.0.0",
    "typescript": "^4.7.4"
  }
}
